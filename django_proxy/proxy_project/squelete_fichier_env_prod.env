ENVIRONMENT=production
SECRET_KEY=#peut se générer avec python3 -c 'import secrets; print(secrets.token_urlsafe(50))' depuis ton venv
DJANGO_ALLOWED_HOSTS=#je te laisse géré les hosts que tu utilises pour le back
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://localhost:4173,http://localhost:5500
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173,http://localhost:5500
#à la fin renomme ce fichier en .env, il sera ignoré par docker et git